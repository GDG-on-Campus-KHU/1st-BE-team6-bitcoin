syntax = "proto3";

package monitoring;
// Go 코드의 패키지 경로를 설정
option go_package = "./proto";

service Bitcoin_Service {
  rpc SayHello (bitcoin_request) returns (stream bitcoin_response);
}

message bitcoin_request {
  string greeting = 1;
}

message bitcoin_response {
  string reply = 1;
}